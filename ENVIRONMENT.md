# Environment and verification boundary

The committed evidence was produced from the pinned Python/`uv` environment
described in `code/pyproject.toml`, `code/uv.lock`, and
`SOURCE_MANIFEST.md`. The lightweight top-level verifier checks committed
artifacts, claim statuses, branch identities, and evidence hashes only.

It does not rerun the full synthetic campaign, ImageNet-C inference, or paid or
remote compute. Claim 6 remains blocked unless the required entropy arrays and
raw Figure 4 tables become available.
